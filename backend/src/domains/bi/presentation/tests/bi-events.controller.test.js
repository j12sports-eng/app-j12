const assert = require("node:assert/strict"); const { test } = require("node:test"); const { BiEventsController } = require("../controllers/bi-events.controller.js");
test("events controller delegates GET query", async () => { let query; const controller = new BiEventsController({ service: { async getAnalytics(input) { query = input; return { readOnly: true }; } } }); const res = response(); await controller.getAnalytics({ query: { period: "TODAY" } }, res, noNext); assert.deepEqual(query, { period: "TODAY" }); assert.equal(res.body.data.readOnly, true); });
test("events controller handles controlled filters", async () => { const controller = new BiEventsController({ service: { async getAnalytics() { throw Object.assign(new Error("invalid"), { code: "BI_FILTER_INVALID", details: { field: "unitId" } }); } } }); const res = response(); await controller.getAnalytics({ query: {} }, res, noNext); assert.equal(res.statusCode, 400); assert.equal(res.body.code, "BI_FILTER_INVALID"); });
function noNext(error) { if (error) throw error; } function response() { return { body: null, statusCode: 200, json(value) { this.body = value; return this; }, status(value) { this.statusCode = value; return this; } }; }
